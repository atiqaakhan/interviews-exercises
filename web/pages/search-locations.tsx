import React from "react";
import SearchLocationComponent from "../modules/search/components/SearchLocations";

const SearchLocation = () => {
  return (
    <SearchLocationComponent />
  );
};

export default SearchLocation;
